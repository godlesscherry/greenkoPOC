spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/test
    username: test
    password: test
  flyway:
    enabled: true
    locations: classpath:db/migration
windfarm:
  emitter:
    enabled: false
  listener:
    enabled: false
  dataset-path: src/test/resources/test-data/sample.csv
